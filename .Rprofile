options(radian.editing_mode = "vi")
options(warnPartialMatchDollar = TRUE)
options(browser = "firefox -P rmarkdown")

if (interactive()) {
  suppressMessages(require(devtools))
  suppressMessages(require(magrittr))
}
